pre-commit:
  parallel: true
  commands:
    golangci-check:
      run: golangci-lint run .
      stage_fixed: true
    golangci-fmt:
      run: golangci-lint fmt {staged_files}
      glob:
        - "*.go"
        - "*.mod"
        - "*.sum"
      stage_fixed: true
