meta {
  name: sign-in
  type: http
  seq: 2
}

post {
  url: {{url}}/auth/sign-in/email
  body: json
  auth: none
}

headers {
  Origin: {{origin}}
}

body:json {
  {
    "email":"{{email}}",
    "password":"{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
