meta {
  name: signup
  type: http
  seq: 1
}

post {
  url: {{url}}/auth/sign-up/email
  body: json
  auth: none
}

headers {
  Origin: {{origin}}
}

body:json {
  {
    "email":"{{email}}",
      "name":"{{email}}",
    "password":"{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
