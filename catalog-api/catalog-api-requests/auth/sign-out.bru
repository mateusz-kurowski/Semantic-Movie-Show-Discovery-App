meta {
  name: sign-out
  type: http
  seq: 3
}

post {
  url: {{url}}/auth/sign-out
  body: none
  auth: inherit
}

docs {
  Requires authentication. Signs out the current session (clears the `better-auth.session_token` cookie).
}

settings {
  encodeUrl: true
  timeout: 0
}
