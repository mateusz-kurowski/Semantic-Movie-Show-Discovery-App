# Root
infra/.env

stack.env
# Per-service local overrides
.env
.env.local
.env.development.local

# MCP server credentials (see .env.mcp.example)
.env.mcp

# Always allow examples
!**/.env.example

/data

design/